' NOTE: If you change the class name "RestFull" here, you must also update the reference to "RestFull" in Web.config and in the associated .svc file.
Public Class RestFull
    Implements IRestFull

    Public Sub DoWork() Implements IRestFull.DoWork
    End Sub

End Class
